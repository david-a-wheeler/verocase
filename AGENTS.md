# ltacproc AGENTS.md

The script `ltacproc.py` is a Python3 script for processing
our extended version of Lightweight Text Assurance Case (LTAC) format
and generating useful results to enable easy documentation and revision
of assurance cases.

The specification of LTAC we implement is in file `docs/ltac-extended.txt`.
